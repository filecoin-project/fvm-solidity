name: Gas Snapshot

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  gas-snapshot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.3.5

      - name: Delete gas snapshot
        run: rm -f .gas-snapshot

      - name: Regenerate gas snapshot
        run: make test

      - name: Check for uncommitted changes
        run: |
          git add .gas-snapshot
          git diff --cached --exit-code .gas-snapshot || {
            echo "Gas snapshot is out of date. Run 'make test' locally and commit the updated .gas-snapshot file."
            exit 1
          }
